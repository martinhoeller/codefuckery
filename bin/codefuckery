#!/usr/bin/env ruby

require 'clamp'
require 'codefuckery'

Clamp do

    option ["--version", "-v"], :flag, "shows codefuckery version" do
        puts "codefuckery #{Codefuckery::VERSION}"
        exit(0)
    end

    def execute
    end
end